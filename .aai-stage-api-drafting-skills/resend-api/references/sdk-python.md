# Python request pattern

Use the current official Resend documentation for SDK installation and request
shapes. Keep `RESEND_API_KEY` in the execution environment, not source or a
skill artifact. A send must receive its sender, recipient, subject, and body
from the current user-authorized objective; do not copy fixed examples into a
live call.
