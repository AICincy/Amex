# Handoff

Package: aai-cognitive-interface
Host: local Codex only
ChatGPT status: NOT-INSTALLED and NOT-ACTIVE

This package ships no active task state, connector inventory, sibling-skill
inventory, credentials, or external facts. On resumption, use the current
request and current host tools as the controlling source.

Validate package changes with `python scripts/run_static_checks.py` from the
package root. A passing result is STATIC-PASS only.
