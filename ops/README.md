# Operator notes

Public redacted state only.

Do not add unpublished numeric floors to this folder. Keep those on the Grok host at `/home/workdir/artifacts/amex-ops-state.yaml` and `/home/workdir/artifacts/r-amex-unpublished-tokens.txt`.

Human gates:

1. Paste `automod/current/r-amex-automod-0.1.3.5.yaml` to `r/amex` wiki `config/automoderator` when authorized.
2. Paste `public/r-amex-rule-1-public-copy.md` into Rule 1 / 1e / monthly-thread header fields.
3. Inspect Crowd Control and Reputation in Safety Filters. Do not treat them as AutoMod.
