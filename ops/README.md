# Operator notes

Public redacted state only: [`amex-ops-state.public.yaml`](amex-ops-state.public.yaml).

Do not add unpublished numeric floors to this folder.

Human gates:

1. Paste `automod/current/r-amex-automod-0.1.3.5.yaml` to wiki `config/automoderator`.
2. Paste `public/r-amex-rule-1-public-copy.md` into Rule 1 / 1e / monthly-thread header fields.
3. Inspect Crowd Control and Reputation in Safety Filters. Do not treat them as AutoMod.
4. Update the public state file after the paste.
