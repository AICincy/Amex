# Package Identity

| Field | Value |
| --- | --- |
| Skill name | authority-currency-auditor |
| Directory name | authority-currency-auditor |
| Role | Subordinate domain module |
| Governor | aai-cognitive-interface |
| Implemented surface | Authority inventory, currentness statuses, Ohio watch-list cross-check |
| Not implemented | Citator completeness, filing, AAI runtime labels |

A hashed export folder such as `skill-<id>/` is a transport wrapper.

Claim STATIC-PASS only after `python3 scripts/aai_runtime_gate.py package <this-directory>` returns PASS in `package-skill` mode.

`current` is an authority-row label for a stated verification timestamp. It is not INSTALLED and not law as of now unless that timestamp is this session against a primary source.
